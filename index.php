<?php
    $items = array("t1","t2","t3","t4");

    foreach($items as $item) {
        echo $item . "<br>";
    };

    //$items[0]; select specific items
    //array_push($foods, "burger"); add items on array
    //array_pop($items) delete items on first index and adjust the index of all items in the current array


?>

